twig:
    paths: ['%kernel.project_dir%/templates','%kernel.project_dir%/../../src/Resources/views']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

services:
    _defaults:
        public: false
        autowire: true
        autoconfigure: true

    Twig\Extra\Intl\IntlExtension: ~
